# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: simple-app
metadata:
  template: simple-template@1.0

services:
  web:
    project: ./src/web
    language: typescript
    host: containerapp
